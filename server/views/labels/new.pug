extends ../layouts/application.pug

block content
  h1.display-4.fw-bold.mt-4= t('views.labels.new.title')

  +formFor()(action='/labels' method='post')
    +floatingFormInput(label, 'name', errors, t('views.labels.new.label'))(type="text")
    +submit(label)(value=t('views.labels.new.submit'))
